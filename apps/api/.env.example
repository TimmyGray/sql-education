# apps/api local env example. Copy to apps/api/.env for Prisma CLI usage
# (Prisma reads apps/api/.env by default). At runtime the app also reads the
# repo-root .env via @nestjs/config. Keep these in sync with the root .env.example.
DATABASE_URL=postgresql://sql_edu:sql_edu_pw@localhost:5432/sql_edu
